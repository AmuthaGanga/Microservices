spring:
  application:
    name: CUSTOMER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/sql_store
    username: root
    password: Amu@Optisol123
    
server:
  port: 9001

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
#  instance:
#    hostname: localhost